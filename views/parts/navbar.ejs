<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <!-- <div class="container"> -->
        <div class="container-fluid">
            <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item ">
                        <a class="nav-link <%= title === 'Home' ? 'active' : '' %>" aria-current="page" href="/">Home</a>
                    </li>
                    
                    <% if (isUser) { %>
                        <li class="nav-item ">
                            <a class="nav-link <%= title == 'Cart'? 'active': '' %>" href="/cart">Cart</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= title == 'Orders'? 'active': '' %>" href="/orders">Orders</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                        <a class="nav-link  <%= title == 'Login'? 'active': '' %>" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= title == 'Signup'? 'active': '' %>" href="/signup">Signup</a>
                    </li>
                    <% } %>
                    
                    <% if (isAdmin) { %>
                        <li class="nav-item">
                            <a class="nav-link <%= title == 'Add Product'? 'active': '' %>" href="/admin/add">Add Product</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= title == 'Manage Product'? 'active': '' %>" href="/admin/orders">Manage
                                Orders</a>
                        </li>
                    <% } %>

                </ul>
                <% if (isUser) { %>
                    <form action="/logout" method="post">
                        <button type="submit" value="Logout" class="btn btn-danger">Logout</button>></button> 
                    </form>
                <% } %>
                
            </div>
        </div>
    <!-- </div> -->
</nav>